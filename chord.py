from notes import NOF
class Chord: 
	def __repr__(self):
		return '{full_name}'.format(full_name = full_name)
	
	def __init__(self, root, third, fifth, add_tones):
		self.root = key[0]
		self.third = key[2]
		self.fifth = key[4]
		self.full_name = root, fifth, third